'use strict';

var assert = require('chai').assert;

describe('Assertion module', function() {
    it('Should support assertions', function() {
        assert.equal(1, 1, 'Basic assertion')
    });
});
